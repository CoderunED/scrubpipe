Done: Day 2 — can modify field values in memory before writing; understand that modifying logs[i]["field"] changes the data that gets written to the file.
Next: Day 3 — write a function to tokenize (replace) email addresses consistently with HMAC.
